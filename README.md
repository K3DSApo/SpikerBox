# SpikerBox
Este programa permite la Visualización correcta de un solo canal del SpikerBox, incluyendo un filtrado que permite la visualización de la señal de la manera más limpia posible
El programa "Serial.ino" debera cargarse a la ESP32
El programa .py se podra ejecutar sin ningun problema solo verificando que el puerto COM corresponda 